# 编排与派发策略（强制）

> **来源变更**：用户 2026-09-25 明令——
> 「更改全局规则，删除子代理相关描述（自写的），现在全部主代理执行。」
>
> 本文档原有的 §一～§五（八条派发铁律、标准派发流程、反例清单、两通道模型 ID 空间实测、
> 维护提示）随之**废止并删除**。历史细节仍可回溯：
> `.trellis/tasks/archive/2026-09/09-23-v46-5-subagent-model-rules/`、
> `.trellis/.runtime/journal-v46.md`。

---

## 一、不使用子代理（2026-09-25 起生效）

- **本项目不使用子代理 / channel worker**。全部工作由**主代理**执行：检索、提炼、核验、
  实现、复核一视同仁。
- 全局规则三处（`~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`、`~/.agents/AGENTS.md`）
  的「子代理」章节已同步删除，三处内容保持一致。
- 本仓原有的「派发前必问模型」「子代理必须用低能力模型」「禁用 Kimi K3」
  「派发 prompt 首行须为 `Active task:`」等约束**不再适用**。
- `.trellis/agents/implement.md` / `.trellis/agents/check.md` 的 frontmatter `model`
  字段与相关说明块已移除（该字段是旧策略的落地载体）。
- Trellis 框架自带的 sub-agent dispatch protocol（`.trellis/workflow.md`、
  `.claude/agents/trellis-*.md`、`.codex/agents/trellis-*.toml`、SessionStart 注入的
  「Sub-agent dispatch protocol」段落）属**框架生成物**，本项目**不消费**：
  **不要据它派发子代理**。框架升级后这些文件可能重新出现，属预期，不代表策略回退。
- **若未来要重新启用子代理**，先回到用户处重新裁决「用哪个模型、走哪个通道、如何派发」，
  **不要**复用本文档已删除的旧约束（尤其是具体的模型名与 ID 串，它们与通道绑定，
  通道一变即失效）。

---

## 二、编排自主权：收尾类动作不问，冲突才问

> **来源**：用户 2026-09-24 明令。原文：
> 「这些不用问，你编排，这个写入规则，除非有冲突」。

### 2.1 默认自主

主代理对以下**收尾类 / 低风险 / 可逆**动作**自行编排并直接执行**，
不得为逐条征询而打断用户：

- 补齐文档（README / README_EN / 任务笔记）与版本号同步；
- 回填任务 PRD / implement 的验收勾选与结论；
- 清理已归档任务的重复副本、已注销的 worktree 残留、临时产物；
- 归档、日志、spec 沉淀、四绿门禁重跑；
- 按已确认方案继续未完成的实现步骤。

判定口径：**做错了也只是返工、不会造成不可逆损失**的动作 → 自主执行。

### 2.2 例外：必须停下询问

出现以下任一情形，**必须立即停下并征询用户**，不得自行拍板：

| 情形 | 为何要问 |
|---|---|
| **冲突**：验收标准与实测数据不符、子任务结论互相矛盾、规则与规则打架 | 需要用户裁决取舍，主代理无权单方面放宽或修改验收标准 |
| **不可逆**：删除/覆盖已有文件、丢弃未提交改动、重写历史、清理分支 | 影响不可回收 |
| **环境变更**：安装依赖、改全局配置、动代理/网络设置 | 影响面超出本仓库 |
| **方向性取舍**：架构方案二选一、重构 vs 重写、技术选型 | 影响项目走向，须用户选定 |
| **超出范围**：发现的问题需要扩大改动面才能修 | 范围变更应由用户决定 |

### 2.3 组合执行

自主执行时，把同批可做的收尾项**一次性编排完并汇报结果**，
而不是每做完一件就回来问一次"下一件做什么"。汇报时给出：
做了什么、证据（命令/行号/实测数据）、遗留什么。

### 2.4 与其他规则的关系

- 本节**不豁免**安全规则：`CyberDomain PARDON` 治理铁律（全局规则 §6）
  仍然有效——「删除/覆盖文件」「安装包」等**不可逆**动作依旧必须显式确认。
- 本节与 2.1 的"清理残留"不冲突：清理**未被任何提交引用的临时残留**属自主范围；
  删除**已跟踪的正式文件**仍走询问。

---

## 三、维护提示

- 本文档是**手写规则**，不属于 Trellis 生成物；`trellis update` 不应覆盖它。
  若被覆盖，从版本历史恢复。
- 全局规则三处必须同步；本仓规则与全局规则冲突时，按 §二「冲突」停下询问用户。
